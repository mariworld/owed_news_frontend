import React from 'react';
import PostContainer from '../ProfileComponents/PostContainer'

const Home = () => (
  <div>
    <h1 id="header">Owed News</h1>
    <PostContainer />
  </div>
);

export default Home;
